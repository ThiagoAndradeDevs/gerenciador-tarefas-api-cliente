/* eslint-disable no-undef */
function Tarefa(ide, nome, concluida) {
  this.id = id;
  this.nome = nome;
  this.concluida = concluida;
  return {
    id: id,
    nome: nome,
    concluida: concluida
  }
}

export default Tarefa;